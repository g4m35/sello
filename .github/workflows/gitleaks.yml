name: Gitleaks

on:
  pull_request:
  push:
    branches: [main, develop]

permissions:
  contents: read
  # gitleaks-action lists PR commits via the API on pull_request events.
  pull-requests: read

jobs:
  scan:
    name: Secret scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # GITLEAKS_LICENSE is only required for GitHub Organization accounts,
          # not for individual/public repositories. Leave unset here.
