# Marketplace revision — September 22, 2026

The owner rejected the September 9 Studio presentation and supplied the actual dark settings screen. The primary failure is truth: Connected · ready hides the distinction between an OAuth connection, selling setup and sales-read permission. The secondary failure is hierarchy: a green sidebar, decorative Studio/Workspace labels, repeated heading stacks and full-width empty cards dominate three account settings.

Hypothesis: explicit selling setup and sales-access states, a readable settings column, and a neutral shared shell let a seller identify the exact blocked action without interpreting a generic ready badge. Existing account scopes and publish checks remain unchanged. No new dependency or generated asset is needed.

Directions considered: (1) compact connection list with inline permission gap, selected; (2) settings tabs per marketplace, rejected because switching hides cross-marketplace state; (3) dashboard of connection cards, rejected because it repeats the space and metadata problems in the supplied screen. Dark and light use neutral surfaces, consistent sans typography, restrained amber only for action-needed states. Preserve normal navigation and native scrolling. Mobile stacks actions below connection facts; native details and existing modal mechanics remain keyboard operable.

Scope: marketplace settings, its existing cards and readiness DTO; shared seller sidebar, spacing, title typography and removal of redundant eyebrows. Preserve seller data, account isolation, all publish/price/consent controls and public marketing. No browser OAuth grant, migration or live marketplace action. This is a reviewable correction; owner visual acceptance remains pending on the new screenshots.

Root cause: eBay omits scope from a successful token response; Sello previously saved an empty list. The existing production account has that empty record, which proves missing metadata, not denied permission. New consent requests sign the exact requested scopes and environment. Successful callbacks use that signed snapshot only when scope is omitted; explicit returned scopes take precedence. Historical empty records remain unverified and require reconnect once after release. No blind permission backfill. Official reference: https://developer.ebay.com/develop/guides/sell/authorization

Acceptance: connected + unknown sales scope says Not verified; connected + missing sales scope shows a targeted reconnect action; connected + granted scope does not claim the worker is healthy; loading/error states do not masquerade as disconnected; expired connection remains distinct. Desktop and exact 390px, dark/light, keyboard, busy/error/retry and read-only fixture checks. Existing registered Dialog, Progress and Camera stay in their real workflows; no quota-driven additions. Motion stays limited to existing state transitions with reduced-motion fallback.
